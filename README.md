# BankTestDrivin

UI Tests should only be run from the provided ordered test "UITest.orderedtest"

You can also select the provided "Ordered.playlist" to run the tests in order.
in the Test Explorer at the top select a playlist "Ordered".
